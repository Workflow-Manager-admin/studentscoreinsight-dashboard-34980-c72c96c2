import React from "react";

/**
 * PUBLIC_INTERFACE
 * Page for listing, adding, editing, and deleting scores.
 */
const Scores = () => {
  // Contents will be implemented next; placeholder for routing/build
  return (
    <section>
      <h1 style={{color: "#1976d2"}}>Scores</h1>
      <div>This page will display scores, allow input/editing, and show history.</div>
    </section>
  );
};

export default Scores;
